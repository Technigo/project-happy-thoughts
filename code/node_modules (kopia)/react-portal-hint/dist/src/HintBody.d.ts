import * as React from "react";
import { ActualPlace, Place } from "./models";
interface Props {
    rect: Readonly<ClientRect>;
    place: Place | ActualPlace[];
    safetyMargin: number;
    rendersSmoothly: boolean;
    shows: boolean;
    bodyClass: string;
    shownClass: string;
    hiddenClass: string;
    usesTransition: boolean;
    onDisappeared?(): void;
}
declare const initialState: {
    contentRect: ClientRect | null;
    onceRendered: boolean;
    transformed: boolean;
};
declare type State = Readonly<typeof initialState>;
declare class HintBody extends React.Component<Props, State> {
    readonly state: State;
    private el;
    private ref;
    private modalRoot;
    private ro;
    constructor(props: Props);
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): React.ReactPortal;
    private onTransitionEnd;
}
export default HintBody;
