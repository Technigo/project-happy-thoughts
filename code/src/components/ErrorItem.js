import React from "react";

const ErrorItem = (error, newThought) => {
  return (
    <div className="error-container">
      if ( {newThought.length < 5 || newThought.length > 140} === {error})
      <label className="error-item" htmlFor="error">
        alert({error})
      </label>
    </div>
  );
};

export default ErrorItem;
