import React from 'react';

export const SendButton = () => {
  const submitInput = () => {
    
  }
  return (
    <button
      type="button"
      className="sendButton"
      onClick={submitInput}>
        ❤️Send a Happy Thought❤️
    </button>
  )
}